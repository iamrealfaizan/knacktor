{
  "schemaVersion": "2.0",
  "slug": "same-tree",
  "number": 100,
  "title": "Same Tree",
  "difficulty": "easy",
  "topics": [
    "tree",
    "binary-tree"
  ],
  "patterns": [
    "dfs"
  ],
  "statement": "Given the roots of two binary trees `p` and `q`, write a function to check if they are the same or not.\n\nTwo binary trees are considered the same if they are structurally identical, and the nodes have the same value.\n\n> Each tree is provided in **level-order array form**: `values[i]` is a node value, or `null` for a missing child (the same format LeetCode uses in its examples).\n\n**Example 1:**\n\n```\nInput: p = [1,2,3], q = [1,2,3]\nOutput: true\n```\n\n**Example 2:**\n\n```\nInput: p = [1,2], q = [1,null,2]\nOutput: false\n```\n\n**Example 3:**\n\n```\nInput: p = [1,2,1], q = [1,1,2]\nOutput: false\n```\n\n**Constraints:**\n\n- The number of nodes in both trees is in the range `[0, 100]`.\n- `-10^4 <= Node.val <= 10^4`",
  "hasVisualization": true,
  "isPremium": false,
  "supportsCustomInput": false,
  "supportsCompare": true,
  "recommendedApproachId": "dfs-compare",
  "inputConstraints": {
    "fields": [
      {
        "name": "p_values",
        "type": "int[]",
        "label": "Tree p (level-order, null for gaps)",
        "placeholder": "1,2,3",
        "min": -10000,
        "max": 10000,
        "minLen": 0,
        "maxLen": 100
      },
      {
        "name": "q_values",
        "type": "int[]",
        "label": "Tree q (level-order, null for gaps)",
        "placeholder": "1,2,3",
        "min": -10000,
        "max": 10000,
        "minLen": 0,
        "maxLen": 100
      }
    ],
    "maxSteps": 2000
  }
}
