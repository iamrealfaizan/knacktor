{
  "id": "bfs-level",
  "name": "Iterative BFS (Level Count)",
  "kind": "alternative",
  "summary": "Sweep the tree level by level with an explicit queue; the number of levels swept is the depth.",
  "visualizationIntent": "init: build and show the input tree; an empty queue aux sits beside it, then the root id enters it and turns amber (frontier). loop: at the start of each level, snapshot how many nodes are queued (level_size) and bump depth by 1; then dequeue exactly that many — a cursor ring lands on cur, the node turns blue (visited), and its real children are enqueued and turn amber, so the queue visibly grows then drains one full level at a time. The number of times the level counter ticks equals the number of tree levels. done: once the queue empties, the depth scalar shows the level count.",
  "complexity": {
    "time": "O(n)",
    "space": "O(n)"
  },
  "language": "python",
  "entrypoint": "Solution.maxDepth",
  "primaryPrimitive": "tree",
  "auxStructures": [
    "queue"
  ],
  "resultSpec": {
    "varName": "depth",
    "label": "MAX DEPTH",
    "suffix": "· levels swept",
    "render": "scalar"
  },
  "varColors": {
    "cur": "current"
  },
  "lineExplanations": {
    "3": "Count how many array slots describe the tree.",
    "4": "Start an empty list of real node objects for the drawing.",
    "5": "Start a lookup from a node's id to its object.",
    "6": "Start a per-node state tracker: unseen, queued, or visited.",
    "7": "Loop over every slot to initialize its state.",
    "8": "Mark this slot unseen (0).",
    "9": "Point the child-slot walker just past the root.",
    "10": "Loop over every slot to build the node objects.",
    "11": "Only non-null slots become real nodes.",
    "12": "Create this node with its value and two empty child links.",
    "13": "Is there a slot left to be the left child?",
    "14": "Is that candidate slot a real (non-null) node?",
    "15": "Link that slot as this node's left child.",
    "16": "Advance the walker past the left-child slot.",
    "17": "Is there a slot left to be the right child?",
    "18": "Is that candidate slot a real (non-null) node?",
    "19": "Link that slot as this node's right child.",
    "20": "Advance the walker past the right-child slot.",
    "21": "Add the finished node to the tree.",
    "22": "Record the node under its id for O(1) lookup.",
    "23": "Create the empty BFS queue.",
    "24": "No node is being visited yet (-1 = none).",
    "25": "Start the level counter at zero.",
    "26": "Seed the sweep only when the tree is non-empty.",
    "27": "Enqueue the root id to start the first level.",
    "28": "Mark the root queued (frontier).",
    "29": "Keep sweeping while the queue holds nodes.",
    "30": "Snapshot how many nodes sit in the current level.",
    "31": "One more level reached — increase the depth by one.",
    "32": "Process exactly that many nodes before moving to the next level.",
    "33": "Dequeue the front node — the one to expand now.",
    "34": "Mark it visited.",
    "35": "Read its left child id.",
    "36": "Read its right child id.",
    "37": "Does it have a real left child?",
    "38": "Enqueue the left child for the next level.",
    "39": "Mark the left child queued.",
    "40": "Does it have a real right child?",
    "41": "Enqueue the right child for the next level.",
    "42": "Mark the right child queued.",
    "43": "Sweep done — clear the current-node cursor.",
    "44": "Return the number of levels — the maximum depth."
  },
  "syntaxExplanations": {
    "1": "class Solution: the wrapper class LeetCode expects.",
    "2": "def maxDepth(self, values): the entry method; values is the tree as a level-order array.",
    "3": "len(values) counts the elements in the input array.",
    "4": "nodes = [] creates an empty list.",
    "5": "by_id = {} creates an empty dictionary (a map from id to node).",
    "6": "state_by_id = [] will hold one status number per node id.",
    "7": "for i in range(n): loops i from 0 to n-1.",
    "8": "append(0) adds a 0 (unseen) entry for this slot.",
    "9": "child = 1 sets the next-child pointer to slot 1.",
    "10": "for i in range(n): a second pass, this time to build nodes.",
    "11": "is not None checks the slot holds a real value.",
    "12": "A dict literal { ... } builds this node object.",
    "13": "if child < n: is there still a slot available?",
    "14": "Check the candidate child slot is not null.",
    "15": "node[\"left\"] = child stores the left child's id.",
    "16": "child = child + 1 advances to the next slot.",
    "17": "if child < n: is there another slot for the right child?",
    "18": "Check the candidate right slot is not null.",
    "19": "node[\"right\"] = child stores the right child's id.",
    "20": "child = child + 1 advances again.",
    "21": "nodes.append(node) adds the built node to the list.",
    "22": "by_id[i] = node registers the node under its id.",
    "23": "queue = [] is a list used as a FIFO queue.",
    "24": "cur = -1 means no node is current yet.",
    "25": "depth = 0 starts the level count at zero.",
    "26": "if n > 0: only sweep a non-empty tree.",
    "27": "append(0) enqueues the root id.",
    "28": "state_by_id[0] = 1 marks the root queued.",
    "29": "while len(queue) > 0: keep going until the queue empties.",
    "30": "level_size = len(queue) records the current level's node count.",
    "31": "depth = depth + 1 counts this level.",
    "32": "for k in range(level_size): loop once per node on this level.",
    "33": "pop(0) removes and returns the front item (dequeue).",
    "34": "state_by_id[cur] = 2 marks this node visited.",
    "35": "read the left-child id into left.",
    "36": "read the right-child id into right.",
    "37": "if left is not None: a real left child exists.",
    "38": "append(left) enqueues the left child.",
    "39": "state_by_id[left] = 1 marks it queued.",
    "40": "if right is not None: a real right child exists.",
    "41": "append(right) enqueues the right child.",
    "42": "state_by_id[right] = 1 marks it queued.",
    "43": "cur = -1 clears the cursor after the sweep.",
    "44": "return depth hands back the maximum depth."
  }
}
