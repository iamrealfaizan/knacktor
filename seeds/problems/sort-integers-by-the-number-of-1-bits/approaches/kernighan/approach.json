{
  "id": "kernighan",
  "name": "Brian Kernighan Bit Count + Selection Sort",
  "kind": "optimal",
  "summary": "Count each number's 1-bits with Brian Kernighan's trick (num &= num-1 clears the lowest set bit, so the loop runs once per 1-bit), then selection-sort by (bit count, value).",
  "visualizationIntent": "init: show the unsorted array with an empty 1-bit key row beneath it. count (phase 1): pointer i walks the array; for each number the inner loop clears the lowest set bit (num & (num-1)) and tallies one, looping once per 1-bit â€” the 'bit ops' counter stays lower than the shift method. The key row fills in each number's 1-bit count. sort (phase 2): grow a sorted prefix to the left of i; scan j across the unsorted right, tracking best (the smallest key so far); the compared element (j) and the best are highlighted. update: best jumps to j when a smaller (bit-count, then value) key is found. move: swap the best element and its key into position i. return: the array is sorted by 1-bit count, then by value.",
  "complexity": {
    "time": "O(n^2 + nÂ·b)",
    "space": "O(n)"
  },
  "language": "python",
  "entrypoint": "Solution.sortByBits",
  "primaryPrimitive": "array",
  "auxStructures": [
    "array"
  ],
  "resultSpec": {
    "varName": "arr",
    "label": "SORTED ARRAY",
    "suffix": "Â· by 1-bit count, then value",
    "render": "list"
  },
  "varColors": {
    "i": "ptr-i",
    "j": "ptr-j",
    "best": "special"
  },
  "lineExplanations": {
    "3": "n is how many integers we must sort.",
    "4": "bit_counts will hold each integer's number of 1-bits, in the same order as arr.",
    "5": "Start the counting index i at 0.",
    "6": "Phase 1 â€” loop over every integer to count its 1-bits.",
    "7": "Copy the current integer into num; we destroy num while counting, so arr is left intact.",
    "8": "count accumulates this integer's 1-bits, starting at 0.",
    "9": "Keep going while num still has at least one 1-bit left.",
    "10": "num & (num - 1) clears the lowest set bit of num â€” the key Brian Kernighan trick.",
    "11": "Exactly one 1-bit was just removed, so add 1 to count.",
    "12": "Record this integer's total 1-bit count in bit_counts.",
    "13": "Move on to the next integer.",
    "14": "Phase 2 â€” reset i to 0 to begin the selection sort.",
    "15": "For each position i, find the element that belongs there.",
    "16": "Assume the element already at i has the smallest key so far.",
    "17": "Start scanning from the next position, j.",
    "18": "Scan the unsorted region looking for a smaller key.",
    "19": "Does arr[j] have fewer 1-bits than the current best?",
    "20": "If so, j becomes the new best.",
    "21": "Otherwise, does arr[j] have the SAME number of 1-bits as best?",
    "22": "On a tie in 1-bits, is arr[j] the smaller value?",
    "23": "If so, j becomes the new best (ties are broken by value).",
    "24": "Advance the scan to the next element.",
    "25": "Save arr[i] before the swap.",
    "26": "Move the best element into position i.",
    "27": "Put the saved value where best was â€” the swap is done.",
    "28": "Save bit_counts[i] so the key row stays aligned with arr.",
    "29": "Move best's key into position i.",
    "30": "Put the saved key where best was â€” the key swap is done.",
    "31": "Position i is settled; advance to the next position to fill.",
    "32": "Return the fully sorted array."
  },
  "syntaxExplanations": {
    "1": "class Solution: â€” the standard wrapper class the platform expects.",
    "2": "def sortByBits(self, arr): â€” arr is the list of integers to sort.",
    "3": "len(arr) counts the elements in the list.",
    "4": "[] creates an empty list.",
    "5": "i = 0 initializes a loop index.",
    "6": "while i < n: repeats while i is still a valid index.",
    "7": "arr[i] reads the element at index i.",
    "8": "count = 0 starts a running total.",
    "9": "while num > 0: loops until num has no set bits left.",
    "10": "num & (num - 1) turns off the lowest set bit of num.",
    "11": "count = count + 1 tallies one set bit per loop iteration.",
    "12": ".append(count) adds count to the end of bit_counts.",
    "13": "i = i + 1 advances the index.",
    "14": "i = 0 resets the index for the second pass.",
    "15": "while i < n: the outer selection-sort loop.",
    "16": "best = i â€” best holds the index of the smallest key found so far.",
    "17": "j = i + 1 â€” j scans the still-unsorted region.",
    "18": "while j < n: scans to the end of the array.",
    "19": "< compares the two integers' 1-bit counts.",
    "20": "best = j updates the best index.",
    "21": "elif runs only when the first if was false (equal 1-bit counts).",
    "22": "< compares the actual integer values.",
    "23": "best = j updates best on a value tie-break.",
    "24": "j = j + 1 advances the scan.",
    "25": "temp = arr[i] stores a value before the swap.",
    "26": "arr[i] = arr[best] overwrites position i.",
    "27": "arr[best] = temp completes the swap.",
    "28": "temp_bits = bit_counts[i] stores the key before its swap.",
    "29": "bit_counts[i] = bit_counts[best] moves the key.",
    "30": "bit_counts[best] = temp_bits completes the key swap.",
    "31": "i = i + 1 moves to the next position to fill.",
    "32": "return arr hands back the sorted list."
  }
}
