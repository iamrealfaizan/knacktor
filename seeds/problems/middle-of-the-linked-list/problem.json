{
  "schemaVersion": "2.0",
  "slug": "middle-of-the-linked-list",
  "number": 876,
  "title": "Middle of the Linked List",
  "difficulty": "easy",
  "topics": [
    "linked-list",
    "two-pointers"
  ],
  "patterns": [
    "fast-slow-pointers"
  ],
  "statement": "Given the `head` of a singly linked list, return the **middle node** of the linked list.\n\nIf there are two middle nodes, return the **second** middle node.\n\n**Example 1:**\n\n```\nInput: head = [1,2,3,4,5]\nOutput: [3,4,5]\nExplanation: The middle node of the list is node 3.\n```\n\n**Example 2:**\n\n```\nInput: head = [1,2,3,4,5,6]\nOutput: [4,5,6]\nExplanation: Since the list has two middle nodes with values 3 and 4, we return the second one.\n```\n\n**Constraints:**\n\n- The number of nodes in the list is in the range `[1, 100]`.\n- `1 <= Node.val <= 100`\n\n**Note on this simulation:** the list is modelled as a values array plus a parallel `node_links` array of next-indexes (`-1` means null), so the chain and every pointer hop are directly visible. Returning a node in LeetCode means returning the rest of the list from that node onward, which is why the answer is a sublist.",
  "hasVisualization": true,
  "isPremium": false,
  "supportsCustomInput": true,
  "supportsCompare": false,
  "recommendedApproachId": "fast-slow-pointers",
  "inputConstraints": {
    "fields": [
      {
        "name": "head",
        "type": "int[]",
        "label": "Linked list values",
        "placeholder": "1,2,3,4,5",
        "min": 1,
        "max": 100,
        "minLen": 1,
        "maxLen": 20
      }
    ],
    "maxSteps": 2000
  }
}
