# Copy this file to .env.local and fill in your values.
# Never commit .env.local — it is already in .gitignore.

MONGODB_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/knacktor?retryWrites=true&w=majority
